# SETUP

### Docker 

Direct Docker Build:
`docker build -t ubc-ocpi-adaptor .`

Tag with a version:
`docker build -t ubc-ocpi-adaptor:latest .`

Docker compose build:

`docker-compose build`
`docker-compose up --build`